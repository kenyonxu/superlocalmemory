{
  "context_servers": {
    "superlocalmemory": {
      "source": "custom",
      "command": "python3",
      "args": ["-m", "superlocalmemory.mcp.server"],
      "env": {
        "PYTHONPATH": "{{INSTALL_DIR}}"
      }
    }
  }
}
