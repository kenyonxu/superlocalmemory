# 运维笔记 · 桥侧「engine not ready」与维护层挂账

> 2026-09-22 上午 · 作者：知惠（由 Kai 转呈）· 状态：**待排查**
> 最近一次盘账：2026-09-21 晚报（本笔记未做新的实测，实测命令待批）

---

## TL;DR

- **引擎本体健康**：daemon v4.2.0 存活、`recall_healthy: true`、embedder 热、DB ~639.6MB；召回日常可用。
- **三笔挂账**（按影响排序）：
  1. **桥侧**：Hermes↔SLM 桥的 `slm_*` 工具通道在（尤其 cron/定时）上下文报 `engine not ready` —— 自 9/17 起未自愈，9/22 为**第 7 天**；
  2. **维护层**：投影队列 ~3,308 条 draining（写侧积压）；维护项 `vector compaction: no orchestrator` 自 **9/13** 连续失败；
  3. **doctor 2 FAIL**：9/13 迁移的失败日志未清（重启可重试）＋ 3,157 条「值得保留」记忆被隐藏、4 条 M043 隔离项（官方口径：重启自修）。
- **首选动作**：一次专项 —— `slm restart`（重试迁移）→ 读迁移日志 → 复跑 doctor → 桥侧 init 核对（见 §5）。

---

## 1. 系统现状（2026-09-21 盘账）

| 项 | 状态 |
|---|---|
| daemon | ✓ v4.2.0，`/health` HTTP 200 |
| 召回 | ✓ `recall_healthy: true`，embedder 热 |
| DB | `~/.superlocalmemory/memory.db` ≈ 639.6MB |
| MCP 工具通道（桥侧） | ⚠️ `engine not ready`（第 6 天，9/22 第 7 天） |
| 投影队列 | ⚠️ ~3,308 条 draining（未归零） |
| doctor | ⚠️ 20 PASS / 2 FAIL |

---

## 2. 挂账 A：桥侧 `engine not ready`

**症状**：cron（含 flush）上下文里 `slm_*` 工具一律返回 `engine not ready`。

**已知线索**：
- 桥代码对 cron 上下文有「跳过模型加载」分支（`integrations/hermes` 的 `initialize()`，`agent_context in {cron, flush}` → 直接 return；此后 `_ensure_engine()` 见 `self._engine is None` → 全部工具 not ready）——**这一半是设计行为**（防 cron 加载大模型）。
- 但 9/18 观察：**工具守卫可能把 daemon-first 路径也一并拦下**——即：即使 daemon 在线、daemon-first 路由本可服务，工具层仍被 `_engine is None` 挡在门外。
- 9/17 结论：问题锁定在 **Hermes↔SLM 桥接侧**（非 daemon），建议查：memory provider 连接日志、daemon 能力文件可见性。

**排查方向**：
1. 读桥侧 provider 初始化日志（谁在什么上下文跳过、跳过是否合理）；
2. 核对工具守卫与 daemon-first 路由的关系：正常会话（非 cron）里 `slm_recall` 是否走 daemon？cron 里是否应放行 daemon 路径？
3. daemon 能力文件（capability file）可见性。

---

## 3. 挂账 B：维护层（`no orchestrator` → 投影积压）

**症状**：
- 日志：`maintenance_scheduler: vector store compaction ... no orchestrator`（日志自带 "This is not a transient failure"）；
- 表现：`projection_outbox` 积压（~3,308 条；最新一批 9/13 入队、落库一度止于 9/8）；
- 归因：维护层缺 orchestrator → vector compaction 不执行 → 写投影（projection_outbox → atomic_facts 材质化）积压。

**判读**：daemon 本体健康、召回正常；属**维护层欠账**（与 §2 的桥侧问题相互独立）。

**排查方向**：maintenance scheduler 的 orchestrator 注入点为何缺位；联动对照 `projection_obligations` / `dead_letter_operations` / `write_commits`。

---

## 4. 挂账 C：doctor 2 FAIL

1. **9/13 迁移失败日志未清** —— doctor 明示：**重启可重试**（对应 9/13 `provenance` 迁移落地那一笔，见 git：`e89b137c`、`93ff8deb`）；
2. **3,157 条「值得保留」记忆被隐藏 ＋ 4 条 M043 隔离项** —— 官方称：**重启自修**。

---

## 5. 建议动作（按序）

1. `slm restart` → **重试 9/13 迁移**；
2. 读迁移日志，确认失败点（若仍败，留档给上游）；
3. 复跑 `doctor`，期望 22/22；
4. 桥侧 init 核对（§2 三条方向）；
5. 观察维护层：重启后 `vector compaction` 是否恢复、`projection_outbox` 是否开始归零。

**验收标准**：
- 正常会话与 cron 会话的 `slm_recall` 均返回真实结果（非 engine not ready）；
- doctor 全绿；
- `projection_outbox` 归零、`atomic_facts` 最新入账日期前进；
- 3,157 条隐藏记忆恢复可见。

---

## 6. 只读快查命令（防锁：timeout + mode=ro）

```bash
# 健康
curl -s http://127.0.0.1:8765/health | python3 -m json.tool | head -40

# 投影积压与义务状态（只读）
sqlite3 -cmd ".timeout 4000" "file:$HOME/.superlocalmemory/memory.db?mode=ro" \
  "SELECT COUNT(*), MIN(enqueued_at), MAX(enqueued_at) FROM projection_outbox;"
sqlite3 -cmd ".timeout 4000" "file:$HOME/.superlocalmemory/memory.db?mode=ro" \
  "SELECT state, COUNT(*) FROM projection_obligations GROUP BY state;"

# daemon 日志（过滤健康探针）
tail -300 ~/.superlocalmemory/logs/daemon.json.log | python3 -c "
import json, sys
for line in sys.stdin:
    try:
        d = json.loads(line.strip())
        if d.get('operation') != 'health_check':
            print(json.dumps(d, ensure_ascii=False)[:200])
    except Exception:
        pass
" | tail -20
```

---

## 7. 红线注记

- cron 里 `engine not ready` **部分属设计行为**——排查时先分清楚「设计跳过」与「真故障」，勿把守卫本身当 bug 修掉；
- 动 daemon 前先看健康（本体健康时，绝大多数问题在桥/维护层，不在引擎）；
- 内存/WAL 注意：SQLite 写入路径的历史教训——**先从关闭路径查**（NO_CKPT_ON_CLOSE 已上；per-call 连接标配）。

---

## 8. 参考坐标

- 账目时间线：知识库 `女仆团/晚报留档/` 2026-09-14 → 2026-09-21 各行「SLM 记忆」；
- 运维手册：`女仆团/运维手册/hindsight-退役迁移手册.md`、知惠侧技能 `slm-operations`（含 §4 召回排障、§四·五 WAL 双端根除、维护层判读）；
- 相关提交（本仓）：`78dfcb50`、`93ff8deb`、`e89b137c`（provenance 落地 9/13）。

*🦊 知惠 · 2026-09-22 · 放在这里，主人顺手可查*
