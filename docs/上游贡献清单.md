# 上游贡献清单(qualixar/superlocalmemory)

> 建立:2026-09-23 · 维护:每次发现新素材或提交后更新此文件
> 定位:滚动跟踪对上游的 PR 机会、bug 报告、以及 fork 独有但可上游化的能力,避免散落在运维笔记/ledger 里被遗忘
> 上游节奏参考:维护者 varun369 响应快(PR #118 三天、#127 两天均 merge,且以自己的 commit 落地+co-author 署名)

## 提交记录

| 时间 | PR/Issue | 内容 | 结果 |
|---|---|---|---|
| 2026-08-14 | PR #118 | WAL close-path 死锁修复(NO_CKPT_ON_CLOSE) | ✅ 4.0.6 落地,co-author + changelog 署名 |
| 2026-09-01 | PR #127 | per-request profile routing(remember/recall 的 profile_id 穿透) | ✅ 4.1.14 落地,8 个 fork 提交全部进上游树 |

## PR 机会(按质量排序)

### P1 · ELC 时钟 bug —— #136 修复不完整 ⭐推荐首发
- **状态**:fork 已修(`f21790dd`,2026-09-23);**判别实验:今晚 02:00 维护窗 archive 不回升即终验**
- **问题**:#136 的 `store_scaled_strength` 时钟修复只落在 `batch_compute_retention`(forgetting_scheduler 路径);维护层 ELC(`dynamics/ebbinghaus_langevin_coupling.py` Step 3)仍调原始 `retention(hours, strength)`,小时制曲线吃真实流逝时间 → 数月未访问的记忆被成批判 forgotten→archive
- **证据链**(PR 描述直接用):曲线真值(3 个月未访问 imp=0.5 → R=0.67 → warm,两路径实测对照);生产实证(2026-09-23 凌晨 02:00 维护窗单 tick 归档 3,281 条 warm 真值记忆;累计 9,107/12,484 = 74% 被误归档);修复后 `slm decay --execute` 全量重算 9,882 条迁移、archive 66%→0
- **修复**:ELC Step 3 同走 `store_scaled_strength`(一行+注释);测试:ELC 与 batch 同输入同 zone 四时间点对表;旧测试 test_low_strength_fades 需按修正契约更新(30 天零重要度:archive→cold)
- **叙事**:"#136 自己的设计原则是'转换只活在一处,两条路径不漂移'——ELC 违反了它"——follow-up 叙事最硬
- **cherry-pick**:`f21790dd` 里 ELC 部分(dynamics 文件+测试),剔除锁容忍部分(可单独提)

### P2 · temporal admission 锁容忍 —— 可用性修复
- **状态**:fork 已修(同 `f21790dd` 的 filter 部分)
- **问题**:`retrieval/temporal_validity_filter.py` 的 correction admission 对所有错误 fail-closed——`database is locked` 瞬断(维护窗内高频)也整卷弃权。实测:一次 5 分钟维护 tick 内 100% 落入的召回全空
- **修复**:锁类错误一次重试(50ms)→ 仍锁 fail-open(跳过该次 admission);非锁保持 fail-closed(设计契约不破坏)
- **注意**:上游可能对 fail-open 有异议(模块注释明示 deliberate fail-closed)——PR 叙事要强调"锁是调度冲突不是完整性答案"的区分,strict_temporal 路径(显式 time-travel)刻意保持 fail-closed 是诚意证明

### P3 · correction_cases 活跃前任幂等化
- **状态**:fork 已修(`9fcb2942` 的 correction 部分,2026-09-22)
- **问题**:`propose_on_connection` 的幂等键只认同一 operation;不同 operation 对同一 predecessor 重复提议直撞 `uq_correction_cases_active_predecessor` 部分唯一索引。生产实证:**每小时 226 条 warning 空转**(累计 1,917 条)
- **修复**:INSERT 前查活跃前任——同 successor 幂等返回既有 case;不同 successor 抛类型化 `CorrectionPredecessorBusyError`(调用方降 debug 日志)

### P4 · 存量 zone 回填工具(文档化或命令)
- **状态**:概念阶段;`slm decay --execute` 实际已可用(本轮 9,882 条迁移就是它做的)
- **机会**:上游修了时钟(#136+我们的 P1)但老库的存量 zone 不会自愈——**每个升级的老库都会撞我们 9-22/9-23 的处境**(召回空转但数据完好)。PR 形态二选一:文档化"升级后建议跑一次 decay --execute";或 `slm db repair-lifecycle` 专命令
- **依赖**:P1 合并后提更有说服力(先修因,再给出路)

## Bug 报告候选(不发 PR 只报)

### B1 · test_hermes_plugin 硬编码 .venv 路径无 skip 守卫
- `test_inventory_matches_the_real_slm_parser_tree` 断言 `REPO/.venv/bin/slm` 存在,无 venv 的环境(任何 worktree)必红;与 enrich 守卫缺失同类

### B2 · test_enrich_new_facts_now 缺无嵌入环境 skip 守卫
- 两个 TestTheVectorIsAttached node id 在无热 embedder 的环境必红(300s 超时);我们每次全量门都 deselect 它们

### B3 · hermes 侧内嵌引擎的 MaintenanceScheduler 噪声
- 非 daemon 进程(如 hermes 内嵌引擎)也会启动 MaintenanceScheduler → `compact_vector_store()` → `get_orchestrator()` 全局为 None → "no orchestrator" warning 刷屏。建议:orchestrator 为 None 时静默跳过 compaction(debug 级),或 scheduler 感知宿主类型

### B4 · projection_outbox 无后端时无出口(设计讨论)
- cozo/lancedb 双 off 时 `ProjectionDrain` 有意不碰任何行("行是待投影工作")——语义正确但队列无界增长+doctor 面积压告警。可开 discussion:无后端配置时应停入队还是定期清空?(我们实测 3,311 条休眠,对召回零伤害)

### B5 · M043 残余 4 条口径不一
- `/health` 报 "4 real memories are hidden from recall and should not be" 但 doctor 21/0 全绿——两个检查的判定口径不一致,小项

## 观察项(数据坐实后再定级)

- **embedder flap**(自 7/28,每 ~5 分钟 semantic DEAD→re-warm):14:47 后零复发,若是 #137 修复的副带效应则无需报;观察一周
- **BM25 覆盖 ~27%**:archive 归零后是否随新写入回升;若不回升则疑有独立 token 清洗 bug
- **通道 8s 超时**:维护搅拌消失后未复发,同 flap 处理

## fork 独有(刻意不上游,记录归属)

- hermes provider(pin_profile/daemon-first/_can_serve cron 服务)——平台耦合
- EmbeddingService daemon fallback(方案 B)——已被 PR #127 部分覆盖语义,剩余 fallback 部分可评估
- embeddings worker proxy 转发 + HF_ENDPOINT 移除(大陆网络特定)
- torch>=2.11.0 宽松 pin(上游有意硬 pin)
- mslm 品牌层

## 操作备忘

- 提交流程:cherry-pick 到 upstream tip 干净分支(剔品牌+fork-only),45 项特性测试在上游树上验证,`gh pr create --repo qualixar/superlocalmemory`
- 判别实验(2026-09-24 早):02:00 维护窗后 lifecycle 分布不回升 → P1 终验达成,可提
